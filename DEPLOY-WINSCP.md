# 🖥️ Deploy ด้วย WinSCP (GUI) - ง่ายกว่า!

## 📥 ขั้นตอนที่ 1: ดาวน์โหลด WinSCP

1. ดาวน์โหลด: https://winscp.net/eng/download.php
2. ติดตั้งตามปกติ

---

## 🔌 ขั้นตอนที่ 2: เชื่อมต่อกับ Proxmox Container

### 2.1 เปิด WinSCP
- คลิก "New Site"

### 2.2 กรอกข้อมูลการเชื่อมต่อ
```
File protocol:  SFTP
Host name:      192.168.44.252
Port number:    22
User name:      root
Password:       <รหัสผ่าน root ของคุณ>
```

### 2.3 บันทึกและเชื่อมต่อ
- คลิก "Save" (ตั้งชื่อ: AI News Server)
- คลิก "Login"
- ถ้ามี Security Warning ให้คลิก "Yes"

✅ **ควรเห็นหน้าต่างแบ่ง 2 ฝั่ง:**
- **ซ้าย**: Windows ของคุณ
- **ขวา**: Server (Proxmox Container)

---

## 📂 ขั้นตอนที่ 3: สร้างโฟลเดอร์บน Server

### 3.1 หน้าต่างขวา (Server):
1. ไปที่ root directory `/`
2. คลิกขวา → "New" → "Directory"
3. ตั้งชื่อ: `opt` (ถ้ามีอยู่แล้ว ข้ามขั้นนี้)
4. เข้าไปในโฟลเดอร์ `opt`
5. คลิกขวา → "New" → "Directory"
6. ตั้งชื่อ: `ai-news-generator`
7. เข้าไปในโฟลเดอร์ `ai-news-generator`

**Path ปลายทาง:** `/opt/ai-news-generator/` ✅

---

## 📤 ขั้นตอนที่ 4: อัปโหลดไฟล์

### 4.1 หน้าต่างซ้าย (Windows):
```
ไปที่: c:\Users\User\OneDrive - Nakhon Phanom University\app\n8n Ai
```

### 4.2 หน้าต่างขวา (Server):
```
อยู่ที่: /opt/ai-news-generator/
```

### 4.3 เลือกไฟล์เหล่านี้จากซ้าย แล้วลากไปขวา:
- ✅ `server.js`
- ✅ `index.html`
- ✅ `package.json`
- ✅ `package-lock.json`
- ✅ `Dockerfile`
- ✅ `docker-compose.prod.yml`
- ✅ `.env.example`
- ✅ `.dockerignore`

**หรือเลือกทั้งหมดแล้วลากทีเดียว!**

### 4.4 รอการอัปโหลด
- Progress bar จะแสดงความคืบหน้า
- รอจนเสร็จทั้งหมด ✅

---

## ⚙️ ขั้นตอนที่ 5: ตั้งค่าบน Server

### 5.1 เปิด PuTTY หรือ Terminal
```
ssh root@192.168.44.252
```

### 5.2 ตรวจสอบไฟล์ที่อัปโหลด
```bash
cd /opt/ai-news-generator
ls -la
```

**ควรเห็นไฟล์:**
```
server.js
index.html
package.json
Dockerfile
docker-compose.prod.yml
.env.example
...
```

### 5.3 สร้างไฟล์ .env
```bash
cp .env.example .env
nano .env
```

**แก้ไขใน nano:**
```env
GEMINI_API_KEY=AIzaSy...  # ใส่ API Key ของคุณ
PORT=3000
NODE_ENV=production
```

**บันทึก:**
- `Ctrl + O` → Enter (บันทึก)
- `Ctrl + X` (ออก)

---

## 🚀 ขั้นตอนที่ 6: Build และ Deploy

### 6.1 Build Docker Image
```bash
docker-compose -f docker-compose.prod.yml build
```
⏳ รอ 2-3 นาที...

### 6.2 Run Container
```bash
docker-compose -f docker-compose.prod.yml up -d
```

### 6.3 ตรวจสอบ Status
```bash
docker ps
```

**ผลลัพธ์ที่ถูกต้อง:**
```
CONTAINER ID   IMAGE              STATUS         PORTS
xxxxx          ai-news-generator  Up 10 seconds  0.0.0.0:3000->3000/tcp
```

### 6.4 ดู Logs
```bash
docker-compose -f docker-compose.prod.yml logs -f
```

**ควรเห็น:**
```
Server running at http://localhost:3000
Connected to SQLite database
✅ Default settings initialized
```

กด `Ctrl + C` เพื่อออกจาก logs

---

## 🌐 ขั้นตอนที่ 7: เข้าใช้งาน

### เปิด Browser ไปที่:
```
http://192.168.44.252:3000
```

### ทดสอบ:
1. ✅ หน้าแรก "สร้างข่าวใหม่" ปรากฏ
2. ✅ คลิก "⚙️ ตั้งค่า" - โหลดค่าเริ่มต้น
3. ✅ อัปโหลดรูปทดสอบ
4. ✅ สร้างข่าว
5. ✅ บันทึกและดูในประวัติ

---

## 🔄 อัปเดตไฟล์ในอนาคต (ด้วย WinSCP)

### วิธีที่ 1: อัปโหลดไฟล์ใหม่
1. เปิด WinSCP
2. Login: AI News Server
3. ไปที่ `/opt/ai-news-generator/`
4. ลากไฟล์ที่แก้ไขแล้วจากซ้ายมาขวา
5. เลือก "Yes" เมื่อถามว่าต้องการ overwrite

### วิธีที่ 2: Restart Container (ใน SSH)
```bash
cd /opt/ai-news-generator
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml build
docker-compose -f docker-compose.prod.yml up -d
```

---

## 📝 Tips สำหรับ WinSCP

### เปิด Text Editor จาก WinSCP
- **Right-click** บนไฟล์ → "Edit"
- แก้ไขโดยตรงบน Server ได้เลย!

### Bookmark Path ที่ใช้บ่อย
- **Ctrl + D** → เพิ่ม bookmark
- ชื่อ: "AI News Project"
- Remote directory: `/opt/ai-news-generator`

### Synchronize ทั้งโฟลเดอร์
- Menu: **Commands** → **Synchronize**
- Local: `c:\...\n8n Ai`
- Remote: `/opt/ai-news-generator`
- เลือก "Preview changes" → OK

---

## ✅ สรุป

| ขั้นตอน | คำสั่ง/Action |
|---------|---------------|
| 1. ดาวน์โหลด | https://winscp.net |
| 2. เชื่อมต่อ | 192.168.44.252, root |
| 3. สร้างโฟลเดอร์ | `/opt/ai-news-generator/` |
| 4. อัปโหลด | ลากไฟล์ 8 ไฟล์ |
| 5. SSH | `ssh root@192.168.44.252` |
| 6. ตั้งค่า | `cp .env.example .env; nano .env` |
| 7. Deploy | `docker-compose -f docker-compose.prod.yml up -d` |
| 8. เข้าใช้ | http://192.168.44.252:3000 |

🎉 **Done!**
