# AI News Generator - คู่มือการใช้งาน

## ✨ ฟีเจอร์หลัก

1. **สร้างข่าวอัตโนมัติด้วย AI** - อ่านข้อมูลจากรูปภาพหรือข้อความแล้วสร้างข่าว
2. **บันทึกประวัติข่าว** - เก็บข้อมูลข่าวที่สร้างไว้ใน database
3. **ตั้งค่าระบบ** - แก้ไข AI model, prompts, และการตั้งค่าต่างๆ ผ่านหน้าเว็บ

## 🚀 วิธีใช้งาน

### เริ่มต้นใช้งาน (Local)
```bash
# ติดตั้ง dependencies
npm install

# สร้างไฟล์ .env และใส่ API key
cp .env.example .env
# แก้ไข: GEMINI_API_KEY=<your-key>

# รันโปรแกรม
npm start

# เปิดเว็บ
http://localhost:3000
```

### หน้าสร้างข่าวใหม่
1. **อัปโหลดรูป/ข้อความ**: เลือกรูปภาพหนังสือราชการ หรือวางข้อความ
2. **ตรวจสอบข้อมูล**: AI จะอ่านข้อมูลอัตโนมัติ แก้ไขได้ตามต้องการ
3. **สร้างข่าว**: กดปุ่ม "สร้างข่าวประชาสัมพันธ์"
4. **บันทึก**: แก้ไขข้อความได้ แล้วกดบันทึกลง DB

### หน้าประวัติข่าว
- ดูข่าวที่สร้างไว้ทั้งหมด
- แก้ไข/ลบข่าว
- ค้นหาข่าว

### หน้าตั้งค่า ⚙️
- **AI Model**: เลือก Gemini 1.5 Flash/Pro/2.0
- **Extraction Prompt**: แก้ไขคำสั่งให้ AI อ่านข้อมูล
- **Generation Prompt**: แก้ไขคำสั่งให้ AI สร้างข่าว
- **ชื่อหน่วยงาน**: ปรับชื่อองค์กร
- **จำนวนข่าวในประวัติ**: กำหนดจำนวนที่แสดง

## 📦 Deploy ไป Proxmox (Docker)

ดูคู่มือฉบับเต็มที่: [DEPLOY.md](DEPLOY.md)

### สรุปสั้นๆ:
```bash
# 1. SSH เข้า Proxmox container
ssh root@<container-ip>

# 2. Clone/Upload โค้ด
git clone <repo> /opt/ai-news-generator
cd /opt/ai-news-generator

# 3. ตั้งค่า .env
cp .env.example .env
nano .env  # ใส่ GEMINI_API_KEY

# 4. รัน Docker
docker-compose -f docker-compose.prod.yml up -d

# 5. เข้าใช้งาน
http://<container-ip>:3000
```

## 🔧 ไฟล์สำคัญ

- `server.js` - Backend API server (Node.js + Express)
- `index.html` - Frontend (Single Page App)
- `news.db` - SQLite Database (สร้างเองอัตโนมัติ)
- `.env` - ตัวแปร environment (ต้องสร้างเอง)
- `Dockerfile` - Docker image configuration
- `docker-compose.prod.yml` - Docker deployment config

## 🎯 Tech Stack

- **Backend**: Node.js, Express, SQLite3
- **Frontend**: HTML, JavaScript, Tailwind CSS
- **AI**: Google Gemini API
- **Deployment**: Docker, Docker Compose

## 📝 License

สำหรับสำนักงานที่ดินจังหวัดนครพนม
