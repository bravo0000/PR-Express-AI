# 📦 วิธี Deploy แบบ Build ไฟล์ไปวาง (Offline Friendly)

วิธีนี้เหมาะมากถ้า Server เน็ตช้า หรือไม่อยากไปยุ่งกับ command line เยอะๆ

## 🖥️ ฝั่ง Windows (ทำครั้งเดียว)

1. ตรวจสอบว่าเปิด **Docker Desktop** ไว้แล้ว
2. ดับเบิ้ลคลิกไฟล์ `build-for-deploy.bat`
3. รอจนเสร็จ... คุณจะได้ไฟล์ชื่อ **`ai-news-app.tar`** (ขนาดประมาณ 200-300MB)

---

## 📤 ฝั่ง Upload (WinSCP)

1. เปิด WinSCP เชื่อมต่อ Server
2. ไปที่ `/opt/ai-news-generator/` (สร้างใหม่ถ้ายังไม่มี)
3. ลาก 3 ไฟล์นี้ไปวาง:
   - 📦 `ai-news-app.tar` (ไฟล์ที่เพิ่งสร้าง)
   - ⚙️ `docker-compose.prod.yml`
   - 🔑 `.env` (ที่มี API Key)

---

## 🚀 ฝั่ง Server (สั่ง Run)

SSH เข้าไปแล้วสั่งแค่ 3 บรรทัดนี้จบเลย:

```bash
cd /opt/ai-news-generator

# 1. โหลดไฟล์เข้า Docker
docker load -i ai-news-app.tar

# 2. รัน (ไม่ต้อง Build แล้ว)
docker-compose -f docker-compose.prod.yml up -d
```

🎉 **เสร็จ! เข้าใช้งานได้เลย**
