# 🔄 วิธีอัปเดตเว็บไซต์ (New Method)

ตั้งแต่วันที่ 29 ม.ค. 2026 ระบบถูกปรับปรุงให้สะดวกขึ้น **ไม่ต้อง Build Image ใหม่ทุกครั้ง**

## 1. วิธีอัปเดตโค้ด (JS / HTML)
เมื่อคุณแก้ไขไฟล์ในเครื่องเสร็จแล้ว ให้ทำดังนี้:

1.  **อัปโหลดไฟล์**: ใช้ WinSCP ลากไฟล์ที่แก้ (เช่น `index.html`, `server.js`) ไปทับไฟล์เดิมบน Server
    *   Path: `/opt/ai-news-generator/`
2.  **สั่งให้เห็นผล**:
    *   ถ้าแก้ **HTML**: กด Refresh หน้าเว็บได้เลย (ทันที!)
    *   ถ้าแก้ **JS (server.js)**: ต้อง Restart Container 1 ครั้ง
        ```bash
        docker compose -f docker-compose.prod.yml restart
        ```

## 2. วิธีอัปเดตใหญ่ (เช่น เปลี่ยน Library / Dockerfile)
กรณีนี้ยังต้อง Build ใหม่เหมือนเดิม:
```bash
cd /opt/ai-news-generator
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d
```
สั่งรันใหม่ 
docker-compose -f docker-compose.prod.yml up -d
