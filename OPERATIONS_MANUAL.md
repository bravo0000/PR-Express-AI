# คู่มือการดูแลระบบและการใช้งาน (Operations & Admin Manual)

เอกสารนี้รวบรวมข้อมูลสำคัญสำหรับการตั้งค่า (Configuration) และการดูแลรักษาระบบ (Maintenance) เพื่อให้ผู้ดูแลระบบคนต่อไปสามารถทำงานต่อได้ทันที

## 1. การตั้งค่าระบบ (Configuration)

### 1.1 ไฟล์ Environment (.env)
ระบบใช้ไฟล์ `.env` ในการเก็บค่าความลับและการตั้งค่าระบบ ห้ามนำไฟล์นี้ขึ้น Git เด็ดขาด
ต้องสร้างไฟล์ `.env` ที่ root directory โดยมีค่าดังนี้:

```properties
# พอร์ตที่ใช้รันเซิร์ฟเวอร์ (Default: 3000)
PORT=3000

# โหมดการทำงาน (development หรือ production)
NODE_ENV=production

# API Key ของ Google Gemini (จำเป็นต้องมี)
# หาได้จาก https://aistudio.google.com/
GEMINI_API_KEY=AIzr............................
```

### 1.2 ฐานข้อมูล (Database)
- **ประเภท**: SQLite (.db file)
- **ตำแหน่งไฟล์**: `./news.db`
- **การจัดการ**: ระบบจะสร้างไฟล์นี้อัตโนมัติเมื่อรันครั้งแรก หากต้องการรีเซ็ตข้อมูล ให้ลบไฟล์นี้แล้ว restart server
- **Backup**: เพียงแค่ Copy ไฟล์ `news.db` เก็บไว้

---

## 2. คำสั่งสำคัญ (Command Line)

### 2.1 การรันแบบ Local (สำหรับแก้ไขโค้ด)
```bash
# 1. ติดตั้ง Library
npm install

# 2. รันโหมด Develop (แก้ไขไฟล์แล้วรีสตาร์ทเอง)
npm run dev

# 3. เข้าใช้งาน
http://localhost:3000
```

### 2.2 การ Deploy ขึ้น Server (Docker)
เราใช้งานผ่าน Proxmox Container (IP: `190.168.xx.xx`) โดยใช้ Docker Compose

**ขั้นตอนการอัปเดตระบบ:**

1. **เชื่อมต่อ Server**:
   ```bash
   ssh root@<SERVER_IP>
   ```

2. **ไปที่โฟลเดอร์โปรเจค**:
   ```bash
   cd /opt/ai-news-generator
   ```

3. **อัปโหลดไฟล์ใหม่** (ถ้าไม่ได้ใช้ Git บน Server):
   ใช้วิธี COPY ไฟล์ผ่าน WinSCP หรือใช้คำสั่ง `scp` จากเครื่องเรา:
   ```powershell
   # ตัวอย่างการอัปโหลดไฟล์ backend
   scp server.js root@<SERVER_IP>:/opt/ai-news-generator/
   ```

4. **Restart Container**:
   เมื่อแก้โค้ดเสร็จแล้ว ต้อง Restart ให้ Docker รับค่าใหม่:
   ```bash
   docker-compose -f docker-compose.prod.yml restart
   
   # หรือถ้ามีการแก้ Dockerfile ให้ Build ใหม่:
   docker-compose -f docker-compose.prod.yml up -d --build
   ```

5. **ดู Logs (ตรวจสอบ Error)**:
   ```bash
   docker-compose -f docker-compose.prod.yml logs -f --tail=100
   ```

---

## 3. โครงสร้างไฟล์สำคัญ (File Structure)

| ไฟล์/โฟลเดอร์ | รายละเอียด | หมายเหตุ |
|--------------|------------|----------|
| `server.js` | แกนหลักของ Backend (Node.js) | จัดการ API, เชื่อมต่อ DB, เรียก Gemini AI |
| `index.html` | หน้าจอ Frontend ทั้งหมด | เป็นไฟล์เดียวรวม HTML/JS/CSS (Single File) |
| `news.db` | ฐานข้อมูล SQLite | ห้ามลบถ้าไม่อยากให้ข้อมูลหาย |
| `Dockerfile` | การตั้งค่า Image ของ Docker | ใช้ Node.js 18-alpine |
| `docker-compose.prod.yml` | Config สำหรับรัน Docker | ใช้ Port 3000, restart: always |

---

## 4. ปัญหาที่พบบ่อย (Troubleshooting)

### A. สร้างข่าวไม่ได้ (Error 500 / AI Error)
*   **เช็ค API Key**: ตรวจสอบใน `.env` ว่า `GEMINI_API_KEY` ถูกต้องและยังไม่หมดโควต้า
*   **เช็ค Logs**: ดู logs ด้วยคำสั่ง `docker-compose ... logs -f` จะเห็น error ชัดเจน

### B. เว็บเข้าไม่ได้ (Connection Refused)
*   **เช็ค Container**: รัน `docker ps` ดูว่า container รันอยู่ไหม
*   **เช็ค Firewall**: ตรวจสอบว่าเปิด Port 3000 หรือยัง

### C. ข้อมูลหาย
*   เช็คว่าไฟล์ `news.db` ถูกลบไปหรือไม่ หรือมีการเปลี่ยน Volume mapping ใน Docker หรือไม่

---

## 🔧 ข้อมูล Server (ลับ)
*(สำหรับผู้ดูแลระบบเท่านั้น)*

*   **IP**: 192.168.44.252
*   **User**: root
*   **Path**: `/opt/ai-news-generator`
